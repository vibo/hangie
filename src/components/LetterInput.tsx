import * as React from "react";

export interface Props {
    letters: string[];
}

export class LetterInput extends React.Component<Props, undefined> {
    render() {
        return (
            <div>
            </div>
        );
    }
}